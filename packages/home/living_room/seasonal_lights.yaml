# seasonal_lights_package:
#   automation:
#     - alias: "Living Room: Toggle window lights"
#       id: cdcabb99-616f-41c1-bdad-cdb9b0f75805
#       mode: queued
#       trigger:
#         - platform: state
#           entity_id: binary_sensor.sun_low_elevation
#         - platform: state
#           entity_id: input_boolean.sleep_mode
#         - platform: state
#           entity_id: light.window_lights
#           from: unavailable
#       action:
#         - choose:
#             # Turn on light if there's activity and sun is low
#             - conditions:
#                 - condition: state
#                   entity_id: binary_sensor.sun_low_elevation
#                   state: "on"
#                 - condition: state
#                   entity_id: input_boolean.sleep_mode
#                   state: "off"
#               sequence:
#                 - service: light.turn_on
#                   data:
#                     entity_id: light.window_lights
#             # Keep on during Christmas days
#             - conditions:
#                 - condition: template
#                   value_template: "{{ now().month == 12 and (now().day == 25 or now().day == 26) }}"
#                 - condition: state
#                   entity_id: input_boolean.sleep_mode
#                   state: "off"
#               sequence:
#                 - service: light.turn_on
#                   data:
#                     entity_id: light.window_lights
#           default:
#             - service: light.turn_off
#               data:
#                 entity_id: light.window_lights
